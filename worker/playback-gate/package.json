{
  "name": "aim-playback-gate",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that gates AIM Studio film delivery from R2 behind short-lived signed tokens.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240605.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.60.0"
  }
}
